[package]
name = "inferd-proto"
description = "Wire format for inferd: NDJSON Request/Response frames, bounded reader, request validation."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
homepage.workspace     = true
authors.workspace      = true
readme = "README.md"
keywords    = ["inferd", "protocol", "llm", "ndjson", "ipc"]
categories  = ["api-bindings", "encoding", "parser-implementations"]

[lib]
# Crate is the canonical schema reference for clients in any language.
# Published to crates.io alongside `inferd-client`.

[dependencies]
serde       = { workspace = true }
serde_json  = { workspace = true }
thiserror   = { workspace = true }
bytes       = { workspace = true }

[dev-dependencies]
# Add proptest / quickcheck here when fuzzing harness lands (Tier 6).
