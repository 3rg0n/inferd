# v0.4 install=work + wire validation

v0.4.0 is **not** releasable until both gates below are green from a
fresh-machine run — no mock backend, no hand-edited config, no
`inferdctl pull` precondition. Run from the release tarball the GitHub
Actions release workflow produces for the matching target triple, **not**
from `cargo build`.

v0.4 carries two distinct risks, so it has two gates:

1. **install=work** — the same fresh-machine → real generate + real
   embed loop v0.3 gated (the accelerator-detection machinery of
   ADR 0019 is unchanged). Re-validated because the wire change touches
   every client path.
2. **wire format** — the v0.4 change ([ADR 0021](adr/0021-unified-v2-wire-length-prefixed-blob-framing.md)):
   one generation socket on a neutral path, length-prefixed type-tagged
   framing, raw BLOB media, in-band `wire_version`. This is the
   load-bearing new surface and must be proven end-to-end against a real
   model, not just in unit tests.

If any step fails, **don't fix in place** — record the failure, file an
issue, and do not cut the release.

## Gate 1 — install=work coverage matrix

Tarballs ship from `.github/workflows/release.yml`. Every shipped target
must be exercised on hardware that can load the matching backend MODULE.
The per-platform procedure is identical to
[`docs/v0.3-validation.md`](v0.3-validation.md) §"Per-platform procedure"
— extract tarball → run install script → `inferdctl doctor` reports the
expected `accelerator=` + `device:` → real generate (≥4 tokens, no
`error` frame) → real embed (vectors, no `error` frame). The only
v0.4 difference in that loop: `inferdctl doctor` now prints
`wire_version=1` on the capabilities line, and the generation socket is
`inferd.sock` / `\\.\pipe\inferd` (not `infer.sock` / `infer.v2.sock`).

| Platform     | Target triple                  | Accelerator | Hardware  | Owner       | Status |
|--------------|--------------------------------|-------------|-----------|-------------|--------|
| Linux x86_64 | `x86_64-unknown-linux-gnu`     | CPU         | any       | Win Claude  | ☑ rc.3 tarball in a no-GPU Wolfi container 2026-06-23: ADR 0019 probe → `accelerator=cpu` on both backends, `status=ready`, `wire_version=1` → real gen (`pong`) + embed (256-dim) |
| Linux x86_64 | `x86_64-unknown-linux-gnu`     | CUDA        | NVIDIA    | Win Claude  | ☑ rc.2 tarball, WSL+RTX5080 2026-06-22 (upgrade rc.12→rc.2 AND fresh-from-nothing: auto-pull ~6GB → ready → real gen+embed) |
| Linux x86_64 | `x86_64-unknown-linux-gnu`     | ROCm        | AMD       | —           | skip |
| Linux x86_64 | `x86_64-unknown-linux-gnu`     | Vulkan      | any GPU   | —           | skip |
| macOS arm64  | `aarch64-apple-darwin`         | Metal       | Apple Si  | Mac Claude  | ☑ rc.3 tarball 2026-06-23: install-launchagent.sh → daemon 0.4.0-rc.3, status=ready, wire_version=1, accelerator=metal, device=MTL0 vram=11.8 GiB → real gen + embed + W1/W3/W4 all green |
| macOS x86_64 | `x86_64-apple-darwin`          | CPU         | Intel Mac | —           | skip |
| Windows      | `x86_64-pc-windows-msvc`       | CPU         | any       | Win Claude  | ☑ (covered by the CUDA row's tarball install on this box) |
| Windows      | `x86_64-pc-windows-msvc`       | CUDA        | NVIDIA    | Win Claude  | ☑ rc.2 tarball, RTX5080 2026-06-22 (upgrade rc.13→rc.2 AND fresh-from-nothing: install.ps1 → auto-pull ~6GB → ready → real gen+embed) |

Rows are reset to ☐ for v0.4 — a v0.3 green does not carry over because
the client/install path changed (socket rename, framing). **skip** rows
are not blockers unless the tarball claims that accelerator.

### Release-build history (the tarballs Gate 1 installs from)

- **v0.4.0 GA (2026-06-23): release run SUCCESS.** All 4 platform
  tarballs + cosign bundles + SBOMs published (`prerelease=false`):
  macOS arm64, Linux x86_64, Linux aarch64, Windows x86_64. GA differs
  from rc.3 only by the doc sweep + the ADR 0022 TCP-deprecation
  doc-comments + additive client conveniences (`DialInfer`,
  `ErrV2WireVersionUnsupported`) — no daemon-runtime change.
- **v0.4.0 GA-tarball smoke (2026-06-24):** SHA-verified the actual
  tagged tarballs, then exercised the real inference path (no mock):
  - **Windows x86_64 CUDA (RTX 5080, this box):** sha256 OK →
    `inferd-daemon 0.4.0` (after flattening `backends/*` next to the
    exe) → booted the **real `llamacpp` backend** from
    `~/.inferd/config.json` (gemma-4-e4b, `n_gpu_layers=-1`) → model
    ready in ~6s → **real generate**: prompt "capital of France" →
    `answer="Paris"`, `backend=llamacpp`, `stop=end_turn`. **Real embed**
    (dedicated embeddinggemma-300m daemon, `--embed`): 256-dim vector
    (MRL), `backend=llamacpp`, `input_tokens=12`. ✅
  - **Linux x86_64 / WSL Ubuntu (this box):** sha256 OK → `inferdctl
    0.4.0` → daemon boots on UDS → length-prefixed v2 wire round-trip →
    terminal `done`. (Wire/transport smoke; the real-model CUDA path on
    this same WSL+RTX5080 host was covered at rc.2/rc.3 — Gate 1 rows
    above.) ✅
  - **macOS arm64**: dispatched to Mac (issue #39).
  An initial mock-backend smoke was run first but **mock is not
  evidence of real inference** — the v1→v2 regressions earlier passed
  mock and broke all real generation. The Windows result above is the
  real `llamacpp` CUDA path on the shipped GA binary.
  Reconfirms the install-flatten requirement (the in-place
  `inferd-daemon --version` fails to load `ggml-base.dll` until
  `backends/*` are co-located, exactly as the packaging README + install
  scripts handle).

- **rc.1 (2026-06-18): release run FAILED.** Windows x86_64 CUDA build
  broke at cmake `enable_language(CUDA)` → "No CUDA toolset found":
  `windows-latest` now resolves to `windows-2025` (VS 2026 / MSVC
  19.51) and CUDA 12.6's `visual_studio_integration` only registers
  MSBuild props for VS 2022. `publish` needs all 4 builds, so no
  release page / tarballs were produced. (Linux/macOS/aarch64 built
  clean.) The 613 MB Linux x86_64 artifact is expected — bundled
  cuBLAS/cuBLASLt redist libs.
- **rc.2 (2026-06-18): Windows matrix pinned to `windows-2022`** so the
  CUDA→MSVC toolset path resolves. All 4 builds green; release page +
  signed tarballs published.
- **Gate 1 run from rc.2 tarballs (2026-06-22, this box, RTX 5080):**
  Windows + WSL/Linux, both the upgrade path (rc.13/rc.12 → rc.2) and
  the fresh-from-nothing path (wipe config + model store → install →
  daemon auto-pulls ~6 GB → ready on CUDA → real generate + embed).
  Three install=work bugs found and fixed running the real path, none
  caught by `cargo`-only validation:
  1. `packaging/windows/install.ps1` still launched the daemon with the
     removed `--v2` / `--v2-addr` flags + the old `\\.\pipe\inferd-infer`
     pipe (the v0.4 consistency sweep fixed systemd/launchd but missed
     the Windows installer) → fresh-install daemon would reject the flag
     and fail to start.
  2. `install.ps1` staged the binary *before* stopping the running
     daemon → upgrade-over-running fails (Windows locks a running .exe).
     Moved the stop+wait before staging.
  3. `uninstall.ps1 -Purge` deleted the install dir without waiting for
     the killed daemon to release its DLL handles → "Access denied" on
     `cublas64_12.dll`. Added a bounded wait after stop.
- **rc.3 (2026-06-22): ships the 3 installer fixes.** All 4 builds green
  (Windows CUDA held on the windows-2022 pin); release page + signed
  tarballs published.
- **Gate 1 re-run from rc.3 tarballs (2026-06-23, this box, RTX 5080):**
  the bundled `packaging/install.ps1` was first verified to actually
  contain the fixes (no `--v2` arg, `$PipePath = \\.\pipe\inferd`,
  stop-before-stage), then run from the tarball. Windows: bundled
  installer → daemon `0.4.0-rc.3` on CUDA, `status=ready`,
  `wire_version=1` → real generate (`pong` / clean `done`,
  `backend=llamacpp`) + real embed (256-dim). WSL/Linux: rc.3 tarball
  staged + booted → same `status=ready` on CUDA → real generate + embed.
  **The shipped installer is now correct.** (sha256 verified on both
  downloads.)
- **macOS (2026-06-23, Mac Claude, #35):** rc.3 tarball via
  `install-launchagent.sh` → `accelerator=metal`, `status=ready`,
  `wire_version=1` → real gen + embed + W1/W3/W4 green.
- **Pure-CPU Linux host (2026-06-23): rc.3 tarball in a Wolfi container
  with NO GPU device exposed** → ADR 0019 probe falls back to
  `accelerator=cpu` on both backends, `status=ready` → real gen + embed.
  NOTE (non-obvious install requirement, already handled by the shipped
  install scripts): ggml's `ggml_backend_load_all()` searches the
  **executable's own directory + CWD**, NOT a `backends/` subdir — so
  the `backends/*` MODULE libs must be **flattened next to the daemon
  binary** at install time. `install.ps1` (Windows) and
  `install-launchagent.sh` (macOS) already do this; a Linux install that
  just extracts the tarball and runs the binary in place (leaving
  `backends/` as a subdir) will load **zero** backends and fail with
  `no backends are loaded` / `llama_model_load_from_file returned null`.
  The systemd unit relies on the operator having staged the binary +
  flattened libs into `~/.local/bin/` per the packaging README.

## Gate 2 — wire-format end-to-end

The framing redesign must be proven through the *full* pipeline: a real
client emits a length-prefixed request frame (+ BlobDescriptor + BLOB
frames for media), the daemon enforces the `wire_version` gate,
reassembles BLOBs by id, and streams length-prefixed response frames
back. Unit/integration tests cover the codec; this gate proves it
against a real model on real hardware.

### W1 — automated cross-language round-trip (per PR, every platform)

`clients/go` `TestEndToEndAgainstDaemon` launches the freshly-built
daemon (mock backend, TCP) and round-trips `GenerateV2` over the
length-prefixed wire. This is the load-bearing cross-language stability
check — it exercises the Go frame codec + `wire_version` against the
Rust daemon with no GPU needed.

- Command: `cargo build -p inferd-daemon && (cd clients/go && go test ./...)`
- Status: ☑ green on Windows x86_64 (this box, 2026-06-13) — also runs in CI.

### W2 — `wire_version` mismatch fails loudly

Send a generation request with a `wire_version` the daemon doesn't speak
(e.g. `0` or `99`); the daemon must reply with a single
`error{code:"wire_version_unsupported"}` frame and close — never parse
the body, never hang.

- Covered by the `unsupported_wire_version_errors_and_closes`
  integration test (`crates/inferd-daemon/tests/v2_stub.rs`): drives a
  bad `wire_version` through the socket and asserts a single
  `Error{WireVersionUnsupported}` frame followed by EOF.
- Status: ☑ automated (Windows x86_64, 2026-06-16; runs in CI).

### W3 — real-model text round-trip (per accelerator that ships)

A real `GenerateV2` text request through a release-tarball daemon with
the default `gemma-4-e4b` backend returns a coherent answer, terminating
with a `done` frame carrying `backend:"llamacpp"` and `stop_reason`.

- Status: ☑ Windows x86_64 CPU (this box, 2026-06-16): real-model text
  through a v0.4 llamacpp daemon over the LP wire, clean `done` with
  `backend:"llamacpp"`. (`tests/echo_llamacpp.rs`, `llamacpp-integration`
  feature, gemma-4-e4b.) This run uncovered + fixed two v1→v2-fold
  regressions — see CHANGELOG "Fixed".
- Status: ☑ Linux x86_64 CPU via WSL Ubuntu (this box, 2026-06-16):
  same `echo_llamacpp` real-model text e2e over the LP wire, green
  (33.7s incl. model load).
- Status: ☑ macOS arm64 Metal (Apple M1, 2026-06-17): `GenerateV2` over
  `${TMPDIR}/inferd/inferd.sock` (LP wire) → `answer="Four"`,
  `backend=llamacpp`, `stop=end_turn`. `go test ./...` (15 tests, 2
  linux-skipped) + `inferdctl doctor` (wire_version=1, accelerator=metal,
  device=MTL0 vram=11.8 GiB) both clean. Two macOS-only test
  regressions found and fixed: `defaultDaemonBin` now picks the
  most-recently-built daemon (release vs debug, newest wins — a
  hardcoded release-first preference broke Windows where a stale v0.3
  release binary sat next to the fresh v0.4 debug build), and the test
  daemon gets HOME isolation.
- Status: ☑ macOS arm64 Metal (Apple M1, 2026-06-23): re-validated from
  the **rc.3 release tarball** (`install-launchagent.sh` → daemon
  `0.4.0-rc.3`, socket `${TMPDIR}/inferd/inferd.sock`, no `--v2` flags).
  `inferdctl doctor` (rc.3): `status=ready`, `wire_version=1`,
  `accelerator=metal`, `device=MTL0 vram=11.8 GiB` on both backends
  (gemma-4-e4b + embeddinggemma-300m). Real embed (256-dim vector).
  `end_to_end_real_inference_over_tcp` green (5.1s, `llamacpp-integration`
  feature). SHA256 of tarball verified OK.

### W4 — raw-BLOB multimodal round-trip (per vision-capable target)

A `GenerateV2` request carrying an image as a **raw BLOB frame** (not
base64) — image content block + matching attachment + BlobDescriptor +
BLOB frame — reaches mtmd and produces a grounded answer. Proves BLOB
reassembly-by-id and the no-base64 media path end-to-end.

- Status: ☑ Windows x86_64 CPU (this box): the v2 image-attachment
  round-trip through mtmd (`tests/llamacpp_multimodal.rs`
  `v2_image_attachment_round_trips`, gemma-4-e4b + mmproj) — the
  attachment's raw bytes reach mtmd via the BLOB path, no base64. Earlier
  (2026-06-13) a 256×256 image sent as one BLOB frame through the daemon
  answered *"A solid red circle is centered on a white background."*
  (in=276, ~250 vision tokens).
- Status: ☑ Linux x86_64 CPU via WSL Ubuntu (this box, 2026-06-16):
  `llamacpp_multimodal` v2 image-attachment round-trip green (2 passed,
  33.8s).
- Status: ☑ macOS arm64 Metal (Apple M1, 2026-06-23): `llamacpp_multimodal`
  `v2_text_only_streams_to_done` + `v2_image_attachment_round_trips` both
  green (2 passed, 33.3s). Image decoded to 256×256 raw RGB, sent as
  attachment bytes through the BLOB wire path (no base64), reached mtmd
  on Metal — `usage.input_tokens > 50` confirmed. mmproj blob:
  `ddf46c…` (990 MB). (`crates/inferd-engine/tests/llamacpp_multimodal.rs`,
  `llamacpp-integration` feature.)

## Release checklist

Before tagging v0.4.0, confirm with the user (no tags without
confirmation), then:

- [ ] Every non-skip row in Gate 1 is ☑ from a release-tarball run.
- [ ] W1 green in CI on every platform.
- [ ] W2 manual spot-check ☑ on at least one release tarball.
- [ ] W3 ☑ on every shipped accelerator target.
- [ ] W4 ☑ on every vision-capable target (CPU counts — vision works on
      CPU, just slower).
- [ ] `CHANGELOG.md` `[Unreleased]` rolled to `[0.4.0]` with the date.
- [ ] Workspace version bumped `0.4.0-dev` → `0.4.0`.

ADR 0021 is already `accepted`; no status flip needed at release.
