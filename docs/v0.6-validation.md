# v0.6 install=work + feature validation

Tracks the "releasable = install=work" gate for the v0.6 line — v0.6.0 GA
below, then **v0.6.1** in its own section at the end: a fresh-machine
installer → `inferdctl doctor` reports `ready` → real `generate` + `embed`
+ the OpenAI-compat bridge, from the **release archive** (no mock, no
hand-edited config, no dev build).

What's new in 0.6.0 vs 0.5.1 (the surfaces this doc must exercise):
- vendored llama.cpp **b9850** + Gemma 4 **12B** support.
- **model auto-selection** (ADR 0023): total-VRAM-gated tier pick
  (≥ `model_autoselect_min_vram_gib`, default 20 → 12B, else E4B).
- **`inferd-http`** OpenAI-compat bridge (ADR 0020), now **with vision**
  (`image_url` → RGB attachment) — bundled in every tarball.
- Windows arm64 was **parked at the 0.6.0 tag** (task #185 — b9850 load
  crash). Fixed and un-parked in 0.6.1; see that section.

## Gate 1 — install=work coverage matrix

Fresh-machine install from the signed release tarball (Windows:
`install.ps1`; Linux: stage + flatten `backends/*`; macOS:
`install-launchagent.sh`) → SHA-256 verify → `inferdctl doctor` = `ready`
→ real generate + embed → the bridge.

| Platform     | Target triple                  | Accel  | Hardware     | Owner      | Status |
|--------------|--------------------------------|--------|--------------|------------|--------|
| Windows      | `x86_64-pc-windows-msvc`       | CUDA   | RTX 5080 16GB| Win Claude | ☑ rc.3 tarball 2026-07-12 (this box): sha256 OK → bundled `install.ps1` upgrade-over-running 0.5.1→0.6.0-rc.3 (stop-before-stage + flatten `backends/*`, 16 DLLs incl. ggml-cuda + CUDA runtime) → `inferdctl 0.6.0-rc.3`, `status=ready`, `accelerator=cuda`, `wire_version=1`; doctor shows both backends distinctly — `gemma-4-e4b vision=true audio=true`, `embeddinggemma-300m embed=true`. `inferd-http.exe` **bundled** (this line's addition). G1–G4 + bridge-from-tarball all green (below). |
| Windows      | `x86_64-pc-windows-msvc`       | CPU    | any          | —          | covered by the CUDA row's tarball install |
| Linux x86_64 | `x86_64-unknown-linux-gnu`     | CUDA   | RTX 5080/WSL | Win Claude | ☑ rc.3 tarball 2026-07-12 (WSL Ubuntu 26.04, this box, **clean env**): sha256 OK → staged binaries + all 26 `backends/*` libs (incl. `libggml-cuda.so` + bundled `libcublas`/`libcudart`) into `~/.local/bin`. Fresh boot wrote first-boot config + **auto-fetched ~6.3 GB** (E4B + mmproj + embed) over the corp network — the ADR 0010 HTTPS bootstrap works, no TLS-revocation block. Daemon `ready` in ~160 s on UDS (`/run/user/1000/inferd/`), CUDA backend dlopen'd, `accelerator=cuda`, both backends distinct (`gemma-4-e4b vision=true audio=true`). G1/G-stream/embed/vision green via the **bundled Linux `inferd-http`** (OpenAI SDK from the Windows side over WSL's localhost mirror). **G3 grammar gap found — see below.** |
| Linux aarch64| `aarch64-unknown-linux-gnu`    | CPU    | any          | —          | tarball built + signed; CPU path unchanged from v0.5 |
| macOS arm64  | `aarch64-apple-darwin`         | Metal  | Apple Si (16 GiB) | Mac Claude | ☑ rc.4 tarball 2026-07-13 (this box): sha256 OK → `install-launchagent.sh` (flattens `backends/*`) → `inferdctl 0.6.0-rc.4`, `inferd-daemon 0.6.0-rc.4`, `inferd-http 0.6.0-rc.4` → `status=ready`, `accelerator=metal`, `wire_version=1`, `device=MTL0 vram=11.8 GiB`; both backends distinct — `gemma-4-e4b vision=true audio=true`, `embeddinggemma-300m embed=true`. 16 GiB unified memory (< 20 GiB threshold) → auto-select correctly picked **E4B**. Full G1–G6 all green (below). |
| Windows arm64| `aarch64-pc-windows-msvc`      | CPU    | WoA          | —          | **parked at the 0.6.0 tag** (task #185): b9850 daemon load crash (`0xC0000135`) even with libomp staged; removed from the release matrix. **Fixed and re-added in 0.6.1** — see that section. |

## Feature gates (per platform, real model, from the installed tarball)

Run against the installed daemon on `\\.\pipe\inferd` (Win) / UDS
(Linux) — no mock, no dev build, no `cargo` test daemon.

### G1 — baseline text
- ☑ Windows x64 CUDA (2026-07-12): "capital of France, one word" →
  `text="Paris"`. First request 65 s (one-time model load / CUDA warmup);
  warm requests instant.
- ☑ macOS arm64 Metal (2026-07-13): "capital of France, one word" →
  `text="Paris"`, `backend=llamacpp`, `stop_reason=end_turn`,
  `input_tokens=22`. Also `end_to_end_real_inference_over_uds`
  (`llamacpp-integration`) green.

### G2 — thinking activation, no channel-token leak
- ☑ Windows x64 CUDA (2026-07-12): "6×7, give the number", `thinking:true`,
  budget 256 → answer `text="42"`, reasoning on the separated `thinking`
  channel, **no `<|channel|>`/`<|start|>`/"channel" markers leaked into
  user-visible text**. ~144 tok/s (full GPU offload; CPU would be ~5).
- ☑ macOS arm64 Metal (2026-07-13): `thinking_activation_yields_thinking_events`
  (`llamacpp-integration`) — "6×7, reason briefly then answer", `thinking:true`
  → reasoning trace on `Thinking` events ("Thinking Process: … 6×7=42 …"),
  user-visible `Text` = `"**Reason:** … **Answer:** 42"`. No channel-token
  leak into text.

### G3 — structured output (grammar)
- ☑ Windows x64 CUDA (2026-07-12): JSON-Schema `response_format`
  {capital, population_millions} → `{"capital":"Paris",
  "population_millions":68}` — parses, conforms to schema.
- ☑ macOS arm64 Metal (2026-07-13): `response_format_constrains_output_to_json`
  (`llamacpp-integration`, native wire) → `{"city": "Paris", "population": 2141000}`
  — valid JSON conforming to schema. `malformed_schema_errors_does_not_crash`
  green — clean error, daemon survives.

### G4 — vision / OCR round-trip (raw-BLOB attachment)
- ☑ Windows x64 CUDA (2026-07-12): 768×420 known-text PNG → decoded to raw
  RGB → BLOB attachment → `input_tokens=165` → transcribed
  `"INFERD V0.6 OCR / token: TOPAZ-9021 / status: VALIDATED"` (both known
  strings recovered).
- ☑ macOS arm64 Metal (2026-07-13): 256×128 known-text PNG ("HELLO INFERD")
  → decoded to raw RGB → BLOB attachment (JSON `BlobDescriptor` + BLOB
  frame, no base64) over the live daemon socket → `input_tokens=73` →
  transcribed `"HELLO INFERD"` — exact match. Also
  `v2_image_attachment_round_trips` (`llamacpp_multimodal`) green.

### G5 — inferd-http bridge from the tarball (new in 0.6)
The bundled `inferd-http` launched against the installed daemon,
driven by the **real `openai` Python SDK** (base_url `/v1`):
- ☑ Windows x64 CUDA (2026-07-12): chat non-stream ("Paris"), chat stream
  ("Tokyo"), embeddings (768-dim), and **vision** (`image_url` data URL →
  transcribed TOPAZ-9021 + VALIDATED) — all pass. `/health` open, `/v1/*`
  served. Bridge is the tarball binary, not a dev build.
- ☑ Linux x64 CUDA / WSL (2026-07-12): same via the bundled Linux
  `inferd-http` (reached from the Windows-side SDK over WSL's localhost
  mirror) — chat, stream, embeddings (768), vision all pass.
- ☑ macOS arm64 Metal (2026-07-13): the tarball's `inferd-http` bound to
  `127.0.0.1:8090`, driven by the real `openai` Python SDK 2.45.0
  (base_url `/v1`): chat non-stream (`"Paris"`, `finish_reason=stop`),
  chat stream (`"1, 2, 3, 4, 5."`), embeddings float (768-dim) + base64
  (valid base64 string) — all pass.

### G6 — structured output through the bridge (found broken, then fixed)
Initially the bridge **silently dropped** OpenAI `response_format`
(`ChatRequest` had no such field; `chat_request_to_v2` never set
`RequestV2.response_format`) — an OpenAI client requesting a JSON schema
got unconstrained text. **Fixed before rc.4**: added `response_format`
(+ `json_schema`) to `inferd-openai-wire` and mapped
`json_schema → RequestV2::JsonSchema{schema}` in the bridge (`text`/
`json_object` carry no schema → unconstrained, by design).
- ☑ Windows x64 CUDA (2026-07-13): OpenAI SDK `response_format`
  `{type:json_schema, json_schema:{schema:{capital,population_millions}}}`
  → `{"capital":"Paris","population_millions":67.7}` — valid JSON
  conforming to the schema, through the rebuilt bridge. Grammar now flows
  end-to-end via the OpenAI surface.
- ☑ macOS arm64 Metal (2026-07-13): same via the bundled `inferd-http` +
  OpenAI SDK — `image_url` data: URL vision → `"HELLO INFERD"` (exact
  transcription of the source PNG); `response_format` json_schema
  {capital, population_millions} →
  `{"capital": "Paris", "population_millions": 2.1458554836533653}` —
  valid, schema-conforming. Both bridge features (vision + grammar) green
  on Metal.

### Known bridge limitation — thinking not reachable via OpenAI surface
`thinking` is an explicit opt-in flag on the v2 wire
(`RequestV2.thinking=true`; omitted/false = no reasoning trace, verified
at `gemma4.rs`). OpenAI's Chat API has no standard "thinking" request
field, so a client **cannot activate thinking through the bridge** today
(native `inferd-client` consumers can). Documented, not a blocker — a
follow-up could map an OpenAI extension/param if wanted.

## Model auto-selection (ADR 0023)
- ☑ Windows x64 CUDA (2026-07-12): 16 GB total VRAM (< 20 GiB threshold) →
  auto-select correctly warms **E4B** (log: `warming gemma-4-e4b …
  total_vram≈17 GB free≈15.7 GB, embed on accelerator`).
- ☐ **12B tier UNVALIDATED**: needs a ≥ 20 GiB accelerator; no such box
  available here. The 12B path has never loaded a real model. GA note.

## Release checklist

Before tagging v0.6.0 GA (no tags without confirmation):

- [x] CI green on `main`.
- [x] rc.3 release run SUCCESS (4 platforms + SBOM + sign/publish).
- [x] Windows x64 CUDA: install=work + G1–G5 from the rc.3 tarball.
- [x] Linux x64 CUDA (WSL): install=work from the rc.3 tarball incl.
      auto-fetch bootstrap; G1/stream/embed/vision + bridge green.
      (Surfaced the G6 bridge-grammar gap, fixed for rc.4.)
- [x] Bridge structured-output (G6): gap found in rc.3, **fixed** in rc.4.
- [x] **rc.4 release run SUCCESS** (4 platforms + SBOM + sign/publish;
      `clients/go/v0.6.0-rc.4` auto-tag pushed; crates.io skipped).
- [x] **rc.4 tarball re-validation (Windows x64)**: SHA OK → the bundled
      `inferd-http.exe` from the rc.4 zip, via OpenAI SDK — chat (Paris),
      **G6 grammar** (`{"capital":"Paris","population_millions":68}`,
      schema-conforming — the fix survives packaging), embeddings (768),
      vision (TOPAZ-9021) — all green.
- [x] rc.4 folds the post-rc.3 `main` commits (#47 log, Go-module tags,
      #49 DialPipe, bridge grammar).
- [x] **macOS arm64 Metal: full G1–G6 from the rc.4 tarball (2026-07-13,
      Mac Claude)**: sha256 OK → `install-launchagent.sh` →
      `status=ready`, `accelerator=metal`, `wire_version=1`,
      `device=MTL0 vram=11.8 GiB`. G1 text (Paris), G2 thinking (42, no
      channel leak), G3 grammar native (Paris/2141000, malformed-schema
      fails closed), G4 vision native (exact OCR transcription,
      `input_tokens=73`), G5 bridge (chat non-stream/stream/embeddings
      float+base64), G6 bridge vision+grammar (exact OCR transcription;
      schema-conforming JSON) — all green. 16 GiB box → auto-select
      correctly picked E4B (< 20 GiB threshold).
- [ ] 12B auto-select tier validated on a ≥ 20 GiB box. **Decision: ship
      GA with this documented-unvalidated** — the auto-select logic is
      unit-tested and the E4B path is proven; only the ≥20 GiB→12B branch
      loading a real 12B model is unexercised (no such hardware here).
- [ ] `CHANGELOG.md` rolled to `[0.6.0]` with the date; workspace version
      bumped off the rc suffix (at GA cut).

## v0.6.1 — post-GA review sweep re-validation

v0.6.1 is a post-GA correctness/security sweep: **F-1** per-request
attachment bounds (`MAX_ATTACHMENTS_PER_REQUEST` 32 /
`MAX_ATTACHMENT_BYTES_PER_REQUEST` 128 MiB), **F-17** bounded response
writes (`--write-timeout-secs`, default 60s), and two engine fixes on
the grammar/sampler path (double `llama_sampler_accept`, hoisted
vocab-sized candidate buffer). **No wire change** — `wire_version`
unmoved, v0.6.0 clients interoperate unchanged.

Release run `30735775875`: **8/8 jobs success** — SBOM, all five platform
legs (including the first successful `aarch64-pc-windows-msvc` release
leg — see the arm64 note below), and sign + publish. All archives,
`.sha256` files and cosign bundles are attached to the release.

**Caveat on that 8/8: the `Publish crates.io` job succeeded without
publishing anything.** `CARGO_REGISTRY_TOKEN` was not set on the repo, and
the job was written to no-op with a notice rather than fail the release — so
that green release run did **not** mean the library crates shipped. Worth
recording because the job list alone would mislead; check the registry API,
not the run summary. (That job no longer exists — see below.)

**Resolved the same day by a manual publish.** `inferd-proto` 0.6.1 and
`inferd-client` 0.6.1 are on crates.io, published from a working tree
verified byte-identical to the `v0.6.1` tag for both crate directories
(`git diff --stat v0.6.1 HEAD -- crates/inferd-proto crates/inferd-client`
empty). Registry API confirms `newest_version = max_stable_version = 0.6.1`,
not yanked, for both.

**The misleading job has since been deleted rather than given a secret.**
`release.yml` no longer has a `crates-io` job at all — a job that reports
success without doing the thing is worse than no job, and a permanent
registry version is a decision worth keeping in a human's hands.
Publishing is manual per `docs/RELEASING.md` §5 (rewritten with the real
two-crate procedure, the tag-identity check, and the registry
confirmation). So from the next tag on there is no crates.io job to
misread — but also nothing automatic: publishing is a step the releaser
must remember.

| Platform     | Target triple                  | Accel  | Hardware          | Owner      | Status |
|--------------|--------------------------------|--------|--------------------|------------|--------|
| Windows      | `x86_64-pc-windows-msvc`       | CUDA   | RTX 5080 16 GB     | Win Claude | ☑ v0.6.1 zip 2026-08-02 (this box): sha256 `b3d57ddf…3ed938` matches the release manifest → bundled `install.ps1 -SourceBinary` exercised the **upgrade-over-running** path (stopped the live 0.6.0-rc.3 daemon, PID 37468, *before* staging), staged the daemon + flattened 16 `backends\*` DLLs, recreated the Startup shortcut, relaunched → `inferd-daemon`/`inferdctl`/`inferd-http` all **0.6.1**, `status=ready` in **1 s**, doctor all `[ ok ]`, `accelerator=cuda`, `device: CUDA0 vram=15.9 GiB`, both backends distinct. Activity NDJSON confirms `"msg":"response write timeout configured","write_timeout":"60s"` (F-17 wired). Native gates **8/8** + bridge gates **6/6** green (below). |
| Linux x86_64 | `x86_64-unknown-linux-gnu`     | CUDA   | RTX 5080 / WSL     | Win Claude | ☑ v0.6.1 tarball 2026-08-02 (WSL Ubuntu 26.04, this box): sha256 `5fe0b9fd…` matches → staged the three binaries + all **26** `backends/*` libs flattened into `~/.local/bin`; all three report **0.6.1**. Booted with the *exact* `packaging/systemd/inferd.service` ExecStart argv — `systemd --user` will not start in this WSL instance (`user@1000.service`: "Device or resource busy"), a WSL defect, so the unit's argv was invoked by hand rather than worked around → `ready` in **5–6 s** on UDS under `/run/user/1000/inferd/`, `admin.sock` `0600`, `inferd.sock`/`infer.embed.sock` `0660`; doctor all `[ ok ]`, `accelerator=cuda`, `device: CUDA0 vram=15.9 GiB`. `response write timeout configured write_timeout=60s` logged. Native gates **8/8** — including **G3 grammar, which closes the gap the 0.6.0 line recorded for this platform** — + bridge gates **6/6**. Model blobs were already in the shared CAS store from the rc.3 run (ADR 0011 blob reuse), so the HTTPS auto-fetch bootstrap was not re-exercised; it is proven in the 0.6.0 rc.3 row above. |
| macOS arm64  | `aarch64-apple-darwin`         | Metal  | Apple Si (16 GiB)  | Mac Claude | ☑ v0.6.1 tarball 2026-08-02 (this box): sha256 OK → `install-launchagent.sh` (flattens `backends/*`) → `inferdctl 0.6.1`, `inferd-daemon 0.6.1`, `inferd-http 0.6.1` → `status=ready`, `accelerator=metal`, `wire_version=1`, `device=MTL0 vram=11.8 GiB`; both backends distinct — `gemma-4-e4b vision=true audio=true`, `embeddinggemma-300m embed=true`. Activity log confirms `response write timeout configured write_timeout=60s` (F-17 wired). 16 GiB unified memory (< 20 GiB threshold) → auto-select correctly picked **E4B**. Full G1–G6 all green (below). |

Windows arm64 (`aarch64-pc-windows-msvc`) is **un-parked in 0.6.1**: the
`0xC0000135` load crash was a missing `libomp140.aarch64.dll` import —
proven with `dumpbin /dependents` on a real arm64 runner after two
DLL-staging attempts failed — resolved by `GGML_OPENMP=OFF` on arm64
(ggml self-threads via its own pthread pool). The leg is back in the
release matrix and built (6m37s) + published here, so the matrix is five
platforms again. Archive **built + signed only** — no WoA hardware
available for install=work. Do not "re-fix" this by staging `libomp.dll`.

On Windows and Linux the gates were driven by a **stdlib-only native-wire
harness** (LEB128 + `[len][type][payload]` per `docs/protocol-v2.md`, raw
NDJSON for embed) rather than a client library, so what is proven is the
normative wire, not one client's reading of it. The same file runs over
UDS and over the named pipe via `CreateFileW` — the pipe is a byte stream,
so the framing code is shared. Both platforms returned identical values.

### G1 — baseline text
- ☑ Windows x64 CUDA + Linux x64 CUDA/WSL (2026-08-02): "capital of
  France, one word" → `text="Paris"`, `type=done`, warm latency **0.48 s**.
- ☑ macOS arm64 Metal (2026-08-02): "capital of France, one word" over
  the live UDS socket → `answer="Paris"`, `backend=llamacpp`,
  `stop_reason=end_turn`, `input_tokens=22`.

### G2 — thinking activation, no channel-token leak
- ☑ Windows x64 CUDA + Linux x64 CUDA/WSL (2026-08-02): "6×7, reason
  briefly then give the number", `thinking:true` → `"42"`,
  `thinking_len=466` on the separated channel, `leaked=[]` (the harness
  asserts none of `<|channel`, `channel|>`, `<|start|>`, `<|think|>`
  reaches user-visible text). Same conclusion as the Metal leg: the
  double-`llama_sampler_accept` removal left the non-grammar decode path
  intact.
- ☑ macOS arm64 Metal (2026-08-02): `thinking_activation_yields_thinking_events`
  (`llamacpp-integration`) — "6×7, reason briefly then answer",
  `thinking:true` → reasoning trace on `Thinking` events
  ("Thinking Process: … 6×7=42 …"), user-visible `Text` =
  `"**Reason:** … **Answer:** 42"`. No `<|channel>`/`<channel|>` leak.
  Confirms the double-`llama_sampler_accept` fix didn't disturb the
  non-grammar decode path.

### G3 — structured output (grammar, native)
- ☑ Windows x64 CUDA + Linux x64 CUDA/WSL (2026-08-02): JSON-Schema
  `response_format` {capital, population_millions} →
  `{"capital": "Paris", "population_millions": 2.1}` — parses, conforms.
  **This closes the G3 grammar gap the 0.6.0 line recorded on Linux/WSL**:
  grammar now passes natively on Linux as well as Windows, from the 0.6.1
  archives, after the sampler/vocab fixes.
- ☑ **G3b** Windows x64 CUDA + Linux x64 CUDA/WSL (2026-08-02): malformed
  schema fails closed and the daemon survives — `{"type":"nonsense"}` with
  `"required": 12345` terminates the request without taking the process
  down, and the next request on a fresh connection returns `done`. This is
  the regression guard for the v0.5-era grammar-sampler abort across FFI.
- ☑ macOS arm64 Metal (2026-08-02): `response_format_constrains_output_to_json`
  → `{"city": "Paris", "population": 2141000}` — valid, schema-conforming.
  `malformed_schema_errors_does_not_crash` → clean error, daemon
  survives. Confirms the grammar-loop fixes (double-accept removal,
  hoisted candidate buffer) didn't regress correctness.

### G4 — vision / OCR round-trip (raw-BLOB attachment)
- ☑ Windows x64 CUDA + Linux x64 CUDA/WSL (2026-08-02): 864×240 known-text
  bitmap rendered directly to **raw RGB** (`w*h*3` = 622 080 B — the
  attachment contract is raw RGB, *not* PNG/JPEG) → JSON `attachment_blob`
  descriptor + `0x02` BLOB frame → `input_tokens=113` → **3/3 strings
  transcribed verbatim**: `"INFERD V061\nTOKEN QUARTZ-7734\nSTATUS
  VALIDATED"`. Glyphs are rendered at scale 8 deliberately — the projector
  downscales toward a token budget and small text drops below legibility
  (#42). Same conclusion as the Metal leg: F-1's new bounds do not
  false-positive-reject a normal single-image request.
- ☑ macOS arm64 Metal (2026-08-02): 256×128 known-text PNG
  ("HELLO INFERD") → decoded to raw RGB → single-image BLOB attachment
  (JSON `BlobDescriptor` + BLOB frame, no base64) over the live daemon
  socket → transcription: **`"HELLO INFERD"`** (exact match),
  `input_tokens=73`. Confirms F-1's new per-request attachment bounds
  (32 attachments / 128 MiB) do not false-positive-reject a normal
  single-image request.

### G4b — F-1 per-request attachment cap (new gate for 0.6.1)
Proves the *new* bound, not just that the old paths still work: a request
declaring **33** attachments (> `MAX_ATTACHMENTS_PER_REQUEST` = 32) must be
rejected before any BLOB is read.
- ☑ Windows x64 CUDA + Linux x64 CUDA/WSL (2026-08-02): 33 declared
  attachments → `type=error`, `code=invalid_request`, `"request declares 33
  attachments; at most 32 allowed"`. The 128 MiB declared-byte budget
  shares the same enforcement point and is covered by the Tier-5 test
  `f1_attachment_table_is_bounded_per_request`.

### F-17 — bounded response writes (new in 0.6.1)
The 60 s default must be live in the **shipped** binary, not only in tests.
- ☑ Linux x64 CUDA/WSL (2026-08-02): startup log
  `response write timeout configured write_timeout=60s`.
- ☑ Windows x64 CUDA (2026-08-02): same, in the activity NDJSON —
  `"msg":"response write timeout configured","write_timeout":"60s"`.
- ☑ macOS arm64 Metal (2026-08-02): same (see the matrix row).
- Gotcha: `INFERD_LOG` is a tracing `EnvFilter` string, so `INFERD_LOG=1`
  filters info records out and this line looks absent. Use
  `INFERD_LOG=info` — which is what `packaging/systemd/inferd.service`
  already sets.

### Embeddings (native NDJSON surface)
- ☑ Windows x64 CUDA + Linux x64 CUDA/WSL (2026-08-02): two inputs with
  `task=retrieval_document` → single terminal `embeddings` frame, `n=2`,
  `dimensions=768`; MRL truncation `dimensions=256` → vectors of length
  **256**.

### G5 — inferd-http bridge from the tarball
- ☑ Windows x64 CUDA (`:8092`) + Linux x64 CUDA/WSL (`:8091`)
  (2026-08-02): each archive's own `inferd-http` against its own daemon,
  driven by `openai` SDK 2.45.0 — **6/6 gates PASS** on each platform.
  G5a chat non-stream (`"Paris"`, `finish_reason=stop`), G5b chat stream
  (multi-chunk, counts 1–5), G5c embeddings float (`n=2`, 768-dim), G5d
  embeddings base64 (decodes to 768 f32), plus G6a/G6b below.
- ☑ macOS arm64 Metal (2026-08-02): the tarball's `inferd-http` bound to
  `127.0.0.1:8091`, driven by the real `openai` Python SDK 2.45.0: chat
  non-stream (`"Paris"`, `finish_reason=stop`), chat stream
  (`"1, 2, 3, 4, 5."`), embeddings float (768-dim) + base64 (valid
  base64 string) — all pass.

### G6 — bridge vision + grammar
- ☑ Windows x64 CUDA + Linux x64 CUDA/WSL (2026-08-02): **G6a bridge
  vision** — `image_url` data URL (PNG, which the bridge decodes to the RGB
  attachment the daemon takes) → 3/3 known strings transcribed; **G6b
  bridge grammar** — `response_format` json_schema {capital,
  population_millions} → schema-conforming JSON. Both green on both
  platforms, from the archives' own bridge binary.
- ☑ macOS arm64 Metal (2026-08-02): `image_url` data: URL (same
  "HELLO INFERD" PNG) → transcription `"HELLO INFERD"` — exact;
  `response_format` json_schema {capital, population_millions} →
  `{"capital": "Paris", "population_millions": 2.1458554836533653}` —
  valid, schema-conforming. Both bridge features green on Metal.

### Model auto-selection (ADR 0023)
- ☑ Windows x64 CUDA + Linux x64 CUDA/WSL (2026-08-02): `device: CUDA0
  vram=15.9 GiB` (< 20 GiB threshold) → **E4B**, as designed. Unchanged
  from 0.6.0.
- ☑ macOS arm64 Metal (2026-08-02): 16 GiB unified memory
  (< 20 GiB threshold) → auto-select correctly picked **E4B**.
- ☐ **12B tier still UNVALIDATED anywhere** — every box available is below
  the 20 GiB threshold (Metal 16 GiB, CUDA 15.9 GiB usable). Carried
  forward from v0.6.0.

### Result
Posture for this patch was **tag first, validate from the artifacts** — the
tag existing did not mean 0.6.1 had shipped, and a failed gate would have
meant cutting v0.6.2, never retagging. No gate failed.

Three platforms validated from the published archives: **Windows x86_64
CUDA**, **Linux x86_64 CUDA (WSL)**, and **macOS arm64 Metal**. All gates
green on all three, no regressions from the F-1 / F-17 / grammar-loop
fixes, and the new behaviour was exercised rather than assumed (G4b
attachment cap; F-17's 60 s default read out of the shipped binary's own
startup log). The 0.6.0-line **G3 grammar gap on Linux/WSL is closed**.
No panic and no write-timeout event fired on any leg. Tracked by issue #53
(Mac) and task #195 (Windows + WSL).

Not covered, stated plainly rather than implied: Windows arm64 and Linux
arm64 install=work (no such hardware here — archives built and signed
only), the ≥ 20 GiB → 12B auto-select branch, and **cosign signature
verification, which was skipped because cosign is not installed on this
box**. SHA-256 was verified against the release manifest for every archive
that was installed.

**Library crates: published.** The release run's crates.io job skipped (see
the `CARGO_REGISTRY_TOKEN` caveat above), so `inferd-proto` 0.6.1 and
`inferd-client` 0.6.1 were published by hand on 2026-08-02 from a tree
verified identical to the tag, and confirmed live against the registry API.
`cargo add inferd-client` now resolves to 0.6.1. Nothing in the validated
install path ever depended on crates.io — the archives carry their own
binaries — so this was a consumer-facing lag, not a gate. Publishing is now a
deliberately manual step for every future tag (task #197 deleted the CI job
that reported success without acting) — see `docs/RELEASING.md` §5.

## Audio attachments — first live validation (post-0.6.1, task #199)

Audio had never run against a real model on any platform. Vision got that
treatment in #182; the audio path was **wire-complete and unproven** —
`Attachment::Audio::sample_rate` was read by nothing and the encoder's
required rate was never queried, so 44.1 kHz PCM fed to a 16 kHz encoder
was accepted and time-scaled ~2.75x into a fluent wrong answer (#198).
This is the gate that had to pass before README/site could advertise audio.

Platform: **Windows x86_64, CPU backend** (`dl-backends` build from
`main`; no CUDA lib staged in this dev build — irrelevant to a wire-contract
proof), Gemma 4 E4B + F16 mmproj. Harness: Go client over
`\\.\pipe\inferd-audio`, ground-truth speech synthesised with Windows SAPI
at 16 kHz mono and parsed to little-endian f32 PCM.

- ☑ **A — the encoder's required rate, read live.** `clip_model_loader: has
  audio encoder` on load, and the admin capabilities frame:
  `{"audio":true,"audio_sample_rate":16000,"backend":"gemma-4-e4b",…}`.
  First real evidence of the shipped mmproj's rate — #198's unit tests only
  *assumed* 16000.
- ☑ **B — matching rate accepted, model hears the audio.** 7.01 s / 112 240
  samples → 448 960 B f32 PCM BLOB at 16000 Hz → `input_tokens=199`,
  `stop=end_turn`, transcript **verbatim**: `"The quick brown fox jumps over
  the lazy dog. The secret pass phrase is tangerine harbor nine."` —
  **5/5** ground-truth phrases. This is content-level proof, not an
  `input_tokens > N` proxy.
- ☑ **C — mismatched rate rejected, both rates named.** Same PCM declared at
  44100 Hz → `code=invalid_request`, `audio attachment "a1": sample_rate
  44100 Hz does not match the model's audio encoder, which requires 16000
  Hz; resample before sending (the daemon does not resample)`.

**Defect found by running it:** neither client library could read the
contract the daemon advertises. `audio_sample_rate` was absent from both
`inferd_client::AdminEvent` and the Go `AdminEvent`, so no supported
consumer could see the rate it is required to honour — and Go had no
`AudioAttachment` constructor, so callers hand-built the struct and could
omit `SampleRate` entirely. Fixed in both clients (`audio_sample_rate`
field, `SupportsAudio`, `RequiredAudioSampleRate`, `AudioAttachment`) with
tests decoding the live frame above verbatim.

Not covered at the time: audio on any platform other than Windows x86_64,
audio on a GPU accelerator, and audio through the `inferd-http` bridge.
The bridge surface is now implemented and validated — next section.

## Audio through the inferd-http bridge (task #200, ADR 0025)

The daemon requires audio at exactly the rate its encoder advertises and
never resamples — so `input_audio` is only a real feature if the *bridge*
converts. ADR 0025 makes that the bridge's job (decode → downmix → resample,
rate read off the admin socket per request). This gate proves a client can
send audio at a rate the daemon would reject and still get a correct answer.

Platform: **Windows x86_64, CUDA** (RTX 5080; `target/release` daemon run
from the installed dir so the CUDA ggml DLLs resolve adjacently), Gemma 4
E4B + F16 mmproj, `audio_sample_rate=16000`. Client: **OpenAI Python SDK
2.45.0** — not curl. Source clip: Windows SAPI at **44 100 Hz stereo**
(2 640 566 B wav), i.e. both the wrong rate *and* the wrong channel count.

- ☑ **Transcription, non-streaming.** 44.1 kHz stereo wav → bridge decode +
  downmix + resample to 16 kHz mono → `input_tokens=397`,
  `finish_reason=stop`, transcript **verbatim**: `"Listen carefully. The
  access code is 4792. The color is magenta. The city is Reykjavik. The
  animal is otter. The number of boxes is 13."` — 5/5 ground-truth items
  (the model writes "13" for the spoken "thirteen"). Content-level proof.
- ☑ **Streaming (SSE).** "What is the access code?" → `'4792'` over 4
  deltas.
- ☑ **Multi-turn.** An audio user turn, an assistant turn, then a *text*
  follow-up ("Which city was mentioned?") → `'Reykjavik'`. The attachment
  stays bound to its own turn.
- ☑ **`format` is a hint only.** Real wav bytes labelled `"format":"mp3"`
  still decode (symphonia probes the container) → `4792`.
- ☑ **Undecodable payload → 400**, not 500 and not a hang: `audio content:
  audio decode failed: unsupported feature: core (probe): no suitable
  format reader found`.
- ☑ **Audio on a `system` message → 400**: `audio content is only allowed
  on \`user\` messages, not \`system\``.
- ☑ **Clip cap is exact.** 5 clips → 400 `too many audio clips in one
  request (max 4)`; 4 clips → 200. (Five copies of the *large* clip return
  413 from the 8 MiB inbound body limit instead — a correct, earlier bound,
  which is why the cap test uses a tiny clip.)
- ☑ **Text-only requests unaffected** and never dial the admin socket.

**Defect found by running it:** pointing the bridge at the **v0.6.1 GA
daemon** (which predates `audio_sample_rate`) produced `400 the daemon's
active backend does not accept audio input`. That is false — the backend
reported `audio:true`; it just advertised no rate — and it sent me auditing
the model instead of the daemon binary. The bridge now distinguishes
"no audio backend" from "audio backend, no advertised rate" and tells the
latter to upgrade the daemon, with five unit tests folding **verbatim live
caps frames** (audio+rate, audio-no-rate, embed-only) so the distinction
can't silently collapse again.

Not covered: audio through the bridge on macOS/Linux, and audio from a
release **tarball** rather than a `cargo` build (both belong to the next
tag's install=work pass).

## Audio on macOS arm64 Metal — native wire + bridge (Mac Claude, 2026-08-04)

Closes the "not covered" gap above for macOS. Platform: **macOS arm64,
Metal** (`cargo build --release -p inferd-daemon --features
dl-backends,metal -p inferd-http -p inferdctl` from `main` at the
workspace's `0.6.1` version — no tarball exists yet for this
still-unreleased work), Gemma 4 E4B + F16 mmproj, `audio_sample_rate=16000`
(confirmed live, matches Windows). Harness: raw LP-wire Python client for
the native tests, real `openai` Python SDK 2.45.0 for the bridge tests.
Ground-truth speech synthesised with macOS `say` (Samantha voice),
converted with `afconvert`.

### Native wire (mirrors task #199)
- ☑ **A — the encoder's required rate, read live.** Admin capabilities
  frame: `{"audio":true,"audio_sample_rate":16000,"backend":"gemma-4-e4b",
  "accelerator":"metal",...}` — identical shape to the Windows result.
- ☑ **B — matching rate accepted, model hears the audio.** 9.66 s / 154 622
  samples → 618 488 B f32 PCM BLOB at 16000 Hz → `input_tokens=260`,
  `stop=end_turn`, transcript **verbatim**: "Listen carefully, the access
  code is 4792, the color is magenta, the city is Reykjavik, the animal is
  otter, the number of boxes is 13." — **5/5** ground-truth items.
- ☑ **C — mismatched rate rejected, both rates named.** Same PCM declared
  at 44100 Hz → `code=invalid_request`, `audio attachment "a1": sample_rate
  44100 Hz does not match the model's audio encoder, which requires 16000
  Hz; resample before sending (the daemon does not resample)`.

### Bridge (mirrors task #200, ADR 0025)
Source clip: macOS `say` at **44 100 Hz stereo** (1 708 800 B wav) — same
wrong-rate + wrong-channel-count shape as the Windows test. Bridge dialed
with `--admin-addr-override` pointed at the test daemon's admin socket
(required for it to read the live sample rate).

- ☑ **Transcription, non-streaming.** 44.1 kHz stereo wav → bridge decode
  + downmix + resample to 16 kHz mono → `finish_reason=stop`,
  `input_tokens=260`, transcript **verbatim**, all 5/5 ground-truth items
  (`4792`, `magenta`, `Reykjavik`, `otter`, `13`).
- ☑ **Streaming (SSE).** "What is the access code?" → `'4792'` over 4
  deltas.
- ☑ **Multi-turn.** Audio user turn → assistant turn → text follow-up
  ("Which city was mentioned?") → `'Reykjavik.'`.
- ☑ **`format` is a hint only.** Real wav bytes labelled `"format":"mp3"`
  still decode (symphonia probes the container) → `4792`.
- ☑ **Undecodable payload → 400**: `audio content: audio decode failed:
  unsupported feature: core (probe): no suitable format reader found`.
- ☑ **Audio on a `system` message → 400**: `audio content is only allowed
  on \`user\` messages, not \`system\``.
- ☑ **Clip cap is exact.** 5 tiny clips → 400 `too many audio clips in one
  request (max 4)`; 4 tiny clips → 200.
- ☑ **Text-only requests unaffected**: `"capital of France, one word"` →
  `"Paris"`.

**Result: every gate from the Windows validation reproduces identically
on Metal.** No defects found — this run is a clean confirmation, not a
discovery pass. `audio_sample_rate` (16000) matches across platforms
because both load the same F16 mmproj; nothing accelerator- or
platform-specific in the decode/resample/rate-check path.

Still not covered: audio from a release **tarball** (this was a `cargo
build --release` dev build, since no tag/tarball exists yet for #199/#200)
— belongs to the next tag's install=work pass, same as Linux.
