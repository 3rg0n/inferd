# v0.8 install=work + feature validation

Tracks the "releasable = install=work" gate for the v0.8 line: a
fresh-machine installer → `inferdctl doctor` reports `ready` → real
`generate` + real `embed` + the OpenAI-compat bridge, from a **release
archive** (no mock, no hand-edited config, no dev build, no "run pull
first").

What's new in 0.8.0 vs 0.7.0 (the surfaces this doc must exercise):

- **`tool_choice` enforced by grammar** (ADR 0029, issue #38) —
  `auto` / `required` / `none` as a constraint, not a hint.
- **`tool_choice_unsatisfied` on the `done` frame** (issue #62) — a
  `required` request that produced no call says so.
- **Breaking behaviour change** — a `tool_result` whose `tool_call_id`
  pairs with no `tool_use` in the same request is rejected with
  `invalid_request` instead of being attributed to a guessed tool.

> **Scope note.** These legs ran **before the `v0.8.0` tag exists**, from
> an archive staged locally with `packaging/stage-release.sh` off the
> release commit — not from a published, signed tarball. That is the one
> respect in which this differs from the v0.6/v0.7 legs, and it is why
> there is no SHA-256 / cosign verification row: there is nothing
> published to verify against yet. Everything downstream of extraction is
> identical to a real install, including running the installer at its real
> default paths.

## Gate 1 — install=work coverage matrix

| Platform      | Target triple               | Accel | Hardware      | Owner      | Status |
|---------------|-----------------------------|-------|---------------|------------|--------|
| Windows       | `x86_64-pc-windows-msvc`    | CUDA  | RTX 5080 16GB | Win Claude | ☑ 2026-08-10 (this box, staged archive): bundled `install.ps1` → exit 0, 15 DLLs flattened adjacent, user `PATH` appended, Startup shortcut registered, daemon launched. `inferd-daemon 0.8.0` / `inferdctl 0.8.0` / `inferd-http 0.8.0`, all reporting `build profile: networked`. `doctor` all-`ok`, `status=ready`, `accelerator=cuda`. Native + bridge gates below; **one defect found and fixed** (G7). |
| Windows       | `x86_64-pc-windows-msvc`    | CPU   | any           | —          | covered by the CUDA row's archive install |
| Linux x86_64  | `x86_64-unknown-linux-gnu`  | CUDA  | RTX 5080/WSL  | Win Claude | ☑ 2026-08-10 (Ubuntu 24.04 WSL, container-built archive): README install steps at real default paths → `systemctl --user enable --now inferd` → `active (running)`, `doctor` all-`ok`, `accelerator=cuda` on `CUDA0`. Native + bridge gates all green. **`systemd --user` exercised for real** (a gap since v0.6.1) and the **ADR 0010 auto-pull run live from an empty store**, which found and fixed a release-blocking defect (below). |
| Linux aarch64 | `aarch64-unknown-linux-gnu` | CPU   | any           | —          | ☐ pending |
| macOS arm64   | `aarch64-apple-darwin`      | Metal | Apple Si (16 GiB) | Mac Claude | ☑ 2026-08-11 (this box, staged archive off `b313f45`): `install-launchagent.sh` → exit 0, `backends/` flattened, plist installed, daemon launched, hit the networked branch (`inferdctl watch`) with a resolvable path per #58. Fresh empty store (config + model store both moved aside first) → all three models auto-pulled cleanly, no SHA mismatch (confirms #64's pinned-revision fix holds here too). `inferd-daemon 0.8.0` / `inferdctl 0.8.0` / `inferd-http 0.8.0`. `doctor` all-`ok`, `status=ready`, `accelerator=metal`, `device=MTL0 vram=11.8 GiB`. Native + bridge gates all green — no new defects. **One environmental finding**: G-B needed a 600s client timeout instead of the harness default 180s on this specific 16 GiB box under memory pressure (see below); not a daemon defect. |
| Windows arm64 | `aarch64-pc-windows-msvc`   | CPU   | WoA           | —          | ☐ pending |

The install was deliberately run at **real default paths** — the existing
v0.6.1 install was moved aside to
`%LOCALAPPDATA%\inferd.v061-backup` rather than redirecting the installer
with `-BinaryPath`. Overriding paths hides exactly the packaging bugs this
gate exists to find (the lesson from #56).

## Feature gates — Windows x86_64 CUDA, 2026-08-10

Run against the installed daemon on `\\.\pipe\inferd` (generation) and
`\\.\pipe\inferd-infer-embed` (embeddings), spoken directly over the wire
by a ctypes named-pipe client rather than through `inferd-client`, so the
framing itself is exercised: `[uvarint payload_len][1 byte type][payload]`,
where `payload_len` counts the payload only (`frame.rs:83`).

### Baseline — the install=work claim

- **generate** — "reply with exactly: INSTALLWORK080" → `text` is
  `'INSTALLWORK080'` verbatim, `stop_reason=end_turn`, `backend=llamacpp`.
- **embed** — 256 dims (MRL truncation), **L2 = 1.000000**.
- **`inferdctl status`** — **exit 0**, three frames relayed with all
  fields. This confirms the #57 / #61 fixes live: `status` reads past
  every capabilities frame instead of stopping at the first.
- **`gpu_layers`** — `43` (E4B) and `25` (embed), i.e. the real offload
  count, confirming the #51 fix on real hardware rather than the clamped
  or over-asked number the previous code published.
- **`audio_sample_rate=16000`** on the audio-capable backend only; absent
  on the embed backend. Correct per ADR 0016 / ADR 0025.

### G-A — `tool_choice: "required"` produces a real call

Cooperative prompt ("What is the weather in Dublin?"), one declared tool:

```
tool_use: {"tool_call_id":"tc-1","name":"get_weather","input":{"city":"Dublin"}}
done:     stop_reason=tool_use, backend=llamacpp, input=68, output=14
```

### G-B — `tool_choice_unsatisfied` fires, adversarially

Prompt engineered against the constraint: *"Do not use any tools. Do not
call any function. Just reply with the single word: hi"*, `required`,
budget 64.

```
text: 'hi\n```\nhi\n```\n'
tool_use count: 0
done: stop_reason=max_tokens, tool_choice_unsatisfied=true
```

This is the measured behaviour ADR 0029 documents rather than a
regression, and it is the whole point of the flag: the grammar makes
*ending the turn* without a call unreachable (so the model cannot answer
in prose and stop), but unlimited non-call text is legal first, so a model
that disagrees with the prompt declines until the budget runs out — which
on its own is indistinguishable from ordinary truncation. The flag makes
it self-announcing, and therefore retryable. Note the degenerate
repetition, which is why raising the budget does not help.

### G-C — the breaking change, live

A `tool_result` citing an id that no `tool_use` in the request created:

```
error: code=invalid_request
message: render: messages[1].content[0]: tool_result references
         tool_call_id "call-that-never-happened", which matches no
         tool_use in this request
```

Rejected, naming the id. Previously this could reach the model attributed
to a guessed tool.

### G-D..G-F — the rejection surface

| Case | Result |
|---|---|
| `tool_choice` with no `tools` array | `invalid_request`: "tool_choice requires a non-empty tools array" |
| `tool_choice` + `response_format` together | `invalid_request`, mutual-exclusion message (only one grammar can be installed) |
| unknown `tool_choice` string | `invalid_request` |

### G-G — `none`, and that text stays reachable

`none` yielded 0 calls, and — the thing worth checking, since `none`
excludes the opener as *text* — prose remained reachable: "2 + 2" → `'4'`,
and a tool-seeking prompt answered in words. A control request with no
`tool_choice` at all made 1 call, so the gate is measuring the field and
not the prompt.

An earlier pass on a tool-seeking prompt returned `output_tokens: 0`,
which looked like `none` had masked everything. It had not: Tier-3
`none_still_answers_an_ordinary_question`
(`crates/inferd-engine/tests/tool_choice_llamacpp.rs:300`) asserts text
stays reachable, and the two follow-up prompts above confirmed it live.
Prompt-specific model behaviour, not a defect.

### G-H — the paired path still works

The non-breaking case, to confirm G-C did not over-reject: `user` →
`assistant`/`tool_use(tc-1)` → `user`/`tool_result(tc-1)` →
`'The weather in Dublin is sunny and 21°C.'`

## Bridge (`inferd-http`) from the archive

| Gate | Result |
|---|---|
| `/health` | `{"status":"ok"}` |
| `/v1/models` | `{"data":[{"id":"inferd",…}],"object":"list"}` |
| `/v1/chat/completions` non-stream | `BRIDGEFIX080` verbatim, `finish_reason: "stop"` |
| `/v1/chat/completions` stream | 5 chunks + `[DONE]` |
| `/v1/embeddings` | 256 dims, L2 1.000000 |
| named-function `tool_choice` | HTTP 400 with the documented message |

### G7 — non-streaming responses dropped tool calls (found here, fixed)

**The one defect this leg found.** `stream: false` with a tool call
returned:

```json
{"choices":[{"finish_reason":"tool_calls","index":0,
  "message":{"content":"","role":"assistant"}}]}
```

`finish_reason` said a call had been made and there was no `tool_calls`
array to execute — so a non-streaming tool-using OpenAI SDK client could
not work at all. The identical request with `stream: true` was correct
throughout, which is why the SDK validation that cleared the bridge in
v0.6.0 missed it: that pass exercised streaming.

Cause: the two paths had diverged. Streaming built its terminal frame from
`ChunkBuilder::finalize`, while the non-streaming handler hand-wrote a
`serde_json::json!` body and discarded the builder it had been feeding
every frame (`let _ = builder;`). The tool calls were buffered and simply
never read. Pre-existing since the bridge shipped in v0.6.0 — deferred by
comment, not a v0.8.0 regression — but `tool_choice` is this release's
headline feature and this was its OpenAI-facing half.

Fixed by giving the non-streaming shape a typed home in
`inferd-openai-wire` (`ChatCompletion` / `CompletionChoice` /
`CompletionMessage`) and rendering **both** paths from the same buffer,
rather than copying the assembly into the second site. A regression test
asserts the two agree on the same generation.

After the fix, same request:

```json
{"object":"chat.completion","choices":[{"index":0,"message":{
  "role":"assistant","content":null,
  "tool_calls":[{"id":"tc-1","type":"function","function":{
    "name":"get_weather","arguments":"{\"city\":\"Dublin\"}"}}]},
  "finish_reason":"tool_calls"}],
 "usage":{"prompt_tokens":72,"completion_tokens":14,"total_tokens":86}}
```

Note `"content": null`, not `""` — OpenAI sends an explicit null on a
tool-call-only turn and clients branch on it.

**Round-trip proof**, which is the claim that actually matters to a
consumer: the `message` object above was posted back verbatim as
conversation history alongside a `role: "tool"` result, and the bridge
completed the loop → `'The weather in Dublin is sunny and 21°C.'` So a
caller can echo a returned choice into the next request unchanged, which
is what every OpenAI SDK's tool loop does.

Streaming re-verified unchanged after the fix (tool call present in the
terminal chunk, `finish_reason: "tool_calls"`, usage intact), and the
text-only non-streaming path re-verified for regression.

## Result — Windows x86_64 CUDA

Green, with one defect found and fixed rather than deferred. Every v0.8.0
surface was exercised against the installed daemon: the new `tool_choice`
enforcement including its adversarial case, the new
`tool_choice_unsatisfied` report, and the breaking `tool_result` rejection
together with the paired path it must not break.

## Feature gates — Linux x86_64 CUDA, 2026-08-10

Host: Ubuntu 24.04 WSL (glibc 2.39, **no CUDA toolkit installed**),
RTX 5080 16 GB via the Windows driver, Python 3.12.3. The absent toolkit
is deliberate: it proves the archive's bundled CUDA redist libs rather
than accidentally resolving against a system install.

### How the archive was built, and why it is not the 26.04 one

The first attempt built on an Ubuntu 26.04 WSL host and **failed to
install** on 24.04:

```
libm.so.6: version `GLIBC_2.43' not found (required by .../libllama.so.0)
```

restart-looping to `Start request repeated too quickly`. `objdump -T` put
the floor precisely: only the vendored C++ libs
(`libllama.so`, `libggml-base.so`, `libggml-cuda.so`) needed `GLIBC_2.43`;
the Rust binaries needed 2.34–2.38. **glibc's floor is set by the build
host**, and 26.04 ships gcc-15 / glibc 2.43.

This is not a release defect: `ubuntu-latest` in `release.yml` resolves to
`Image: ubuntu-24.04` (confirmed in run `31049725450`'s log), so published
tarballs carry the 2.39 floor. But it made *my* archive unrepresentative,
which defeats the gate — so rather than validate around it, the archive
was rebuilt in **`docker.io/library/ubuntu:24.04` with CUDA 12.6**, i.e.
CI's exact image and CUDA pin. Floor dropped to `GLIBC_2.38` across all
six artifacts. Archive: 1,077,177,974 bytes, SHA-256
`23933fcc6ebea9a98785d5e4b26a7039003708f339f22d501857d2c28f3793e6`.

Choosing 24.04 also unlocked the two things below: 26.04 WSL cannot spawn
any systemd user manager here (`user@1000.service` → `failed (Result:
resources)`, reproduced on a fresh boot), so the unit file could not have
been exercised there at all.

`release.yml`'s own artifact gates were re-run locally against the built
tree: `--version` on all three binaries, `--backend llamacpp` present in
the CLI surface, `backends/` containing `libggml-cuda.so` (proving the
CUDA build did not silently degrade to CPU-only), and the readelf-BFS
DT_NEEDED closure resolving under `backends/` + the driver skiplist
(`libcuda.so.1`, `libnvidia-ml.so.1` — EULA-forbidden to redistribute) +
system libs.

### Install — `systemd --user`, exercised for the first time since v0.6.1

Installed via the documented Linux steps from `README.md` verbatim (no
path overrides), then:

```
Active: active (running) since Mon 2026-08-10 15:55:45 BST
Process: ExecStartPre=/usr/bin/mkdir -p …/.inferd …/.local/share/models
         (code=exited, status=0/SUCCESS)
single-instance lock held  path=/run/user/1000/inferd/inferd.lock
admin uds listener bound   path=/run/user/1000/inferd/admin.sock
no config file at default path; wrote first-boot default
```

The v0.6.1 Linux row records that `systemd --user` would not start under
WSL, so the unit's argv was invoked by hand. **That gap is now closed**:
`systemctl --user enable --now inferd` ran for real, which is also what
proves the #59 fix live — `ExecStartPre` exits `0/SUCCESS`, meaning the
`+` prefix correctly hoists the `mkdir` out of the sandbox whose
`ReadWritePaths=` targets it creates. The admin socket bound before the
model work, per invariant #5.

### The defect this leg found: `auto_pull` was broken on any empty store

This host started with an **empty model store**, so unlike the Windows leg
(and unlike v0.6.1, whose row notes the blobs were already present) the
ADR 0010 HTTPS bootstrap actually ran. Copying blobs from the 26.04
store would have been faster and legitimate under ADR 0011; it was
declined precisely so this path got exercised.

It failed:

```
model download progress downloaded_mib=4888 total_mib=4888
ERROR backend init failed — daemon is shutting down
  error=fetch failed for gemma-4-e4b: SHA-256 mismatch
  (expected 30d1e794…, got 3cf61de1…); quarantined to
  …/locks/quarantine/20260810T151705Z-sha-mismatch.bin
systemd: Scheduled restart job, restart counter is at 1
```

— then downloaded the same 5 GB again, on a loop. The quarantined file
was 5,126,306,944 bytes against a pinned 5,126,304,928: a **different
artifact**, not a corrupt transfer. HuggingFace's `X-Linked-ETag` for
`resolve/main/` returned `3cf61de1…`, byte-identical to what the daemon
computed, confirming the download was perfect and upstream had changed:
unsloth re-quantised both Gemma 4 text GGUFs on 2026-07-17 ("Added Gemma
official chat template update"). The 12B pair had drifted identically;
both mmproj projectors and the embedding model had not.

The verification worked exactly as designed — the bug was pinning a
content digest against a mutable branch URL, an unsatisfiable pair once a
publisher re-quantises in place. Fixed by pinning immutable 40-char repo
revisions in every shipped default, each verified via `X-Linked-ETag` to
serve bytes matching the **already-pinned** digests, so no SHA and no
model behaviour changed. Two unit tests now assert the invariant
structurally, and both were confirmed to fail against the old URLs.
See `CHANGELOG.md` under 0.8.0 → Fixed.

After the fix, from the same empty store: all three blobs fetched and
SHA-verified, `backend ready`, both inference sockets bound.

### Baseline — the install=work claim

Spoken directly over the wire by a raw UDS client (not `inferd-client`),
so the framing is exercised: `[uvarint payload_len][1 byte type][payload]`
on `/run/user/1000/inferd/inferd.sock`, NDJSON on `infer.embed.sock`.

- **generate** — `'INSTALLWORK080'` verbatim, `stop_reason=end_turn`,
  `backend=llamacpp`.
- **embed** — 256 dims (MRL truncation), **L2 = 1.000000**.
- **`inferdctl status`** — exit 0, **both** capabilities frames relayed
  with all fields plus the `ready` frame (#57 / #61 live).
- **`inferdctl doctor`** — every check `ok`, including blob + manifest
  presence for both models and `accelerator=cuda`, `device: CUDA0
  vram=15.9 GiB`.
- **`gpu_layers`** — `43` (E4B) / `25` (embed), matching Windows exactly
  and confirming the #51 fix is host-independent.
- **`audio_sample_rate=16000`** on the audio-capable backend only.
- Accelerator: `using device CUDA0 (NVIDIA GeForce RTX 5080)`, layers
  assigned to `CUDA0` — the bundled redist libs loaded with no toolkit
  present.

### G-A..G-H — identical results to the Windows leg

| Gate | Result |
|---|---|
| G-A `required` → real call | `get_weather({"city":"Dublin"})`, `stop_reason=tool_use` |
| G-B adversarial `required` | `text='hi\n```\nhi\n```\n'`, 0 calls, `stop_reason=max_tokens`, **`tool_choice_unsatisfied=true`** |
| G-C unpaired `tool_result` | `invalid_request`, naming `"call-that-never-happened"` |
| G-D `tool_choice` without `tools` | `invalid_request`: "requires a non-empty tools array" |
| G-E `tool_choice` + `response_format` | `invalid_request`, mutual-exclusion message |
| G-F unknown `tool_choice` string | `invalid_request`: "must be one of: auto, required, none" |
| G-G `none` | 0 calls; control with no `tool_choice` made 1 call; "2 + 2" → `'4'` so text stayed reachable |
| G-H paired `tool_result` | `'The weather in Dublin is sunny and 21°C.'` |

G-B reproduced the ADR 0029 behaviour byte-for-byte including the
degenerate repetition — cross-platform, so a property of the constraint
rather than of one host.

### Bridge (`inferd-http`) from the archive

| Gate | Result |
|---|---|
| `/health` | `{"status":"ok"}` |
| `/v1/models` | `{"data":[{"id":"inferd",…}],"object":"list"}` |
| non-stream chat | `BRIDGE080LINUX` verbatim, `finish_reason: "stop"` |
| stream chat | 34 data lines ending `[DONE]` |
| `/v1/embeddings` | 256 dims, L2 1.000000 |
| **G7 `stream:false`** | `tool_calls` array present, `content: null`, `finish_reason: "tool_calls"` |
| **G7 `stream:true`** | terminal chunk carries `tool_calls`, `finish_reason: "tool_calls"` |
| G7 round trip | returned `message` echoed back with a `role:"tool"` result → `'The weather in Dublin is sunny and 21°C.'` |
| named-function `tool_choice` | HTTP 400 with the documented message |

The `3b75846` non-streaming fix — the defect the Windows leg found — is
confirmed on a second platform, from the archive binary, on both paths.

### Note on the gate scripts

Two harness bugs were found and fixed here, neither a daemon defect, both
worth recording because they produced convincingly wrong output:

- The client sent `wire_version: 2` and got a correct
  `wire_version_unsupported` on **every** generation gate. The "v2" in the
  surface name is the *generation surface* generation;
  `inferd_proto::v2::WIRE_VERSION` is **1** and has never moved (ADR 0021).
- Frame extraction expected `{"type":"token","text":…}`. The real shape is
  `{"type":"frame","block":{"type":"text","delta":…}}`, so every gate
  printed empty text and zero tool calls while the terminal frames were
  already correct — a reminder that a green terminal frame is not a green
  gate.

## Result — Linux x86_64 CUDA

Green, and it earned its keep: it found the one defect that would have
made a fresh v0.8.0 install unusable on any machine without a pre-warmed
model store — which every prior leg had, and so could not have caught.

## Feature gates — macOS arm64 Metal, 2026-08-11

Host: Apple Silicon, 16 GiB unified memory, Metal accelerator. Built from
`b313f45` on `main` (`git submodule update --init --recursive` was
required first — the submodule pointer had drifted to an unrelated
`llama.cpp` commit from an earlier session and the stale FFI surface
failed to compile with `cannot find function llama_model_n_layer_nextn`
until re-synced to the pinned `4f31eedb0`). `cargo build --release` for
all three binaries with the flags the issue specified; `libclang.dylib`
from Xcode Command Line Tools was sufficient for bindgen, no `brew
install llvm` needed. `ls backends/ | grep libggml-metal` confirmed
`libggml-metal.so` present before staging — the build did not silently
degrade to CPU-only. `packaging/stage-release.sh` staged the archive
without complaint.

### Install — real default paths, genuinely empty store

The box's existing v0.7.0 install was stopped (`launchctl bootout`) and
moved aside, and — following the issue's preference for an empty store
when affordable — the existing `~/.inferd/config.json` (which still
named unpinned `resolve/main/` URLs, predating #64's fix) and the entire
model store were moved aside too, rather than reused. `install-launchagent.sh`
flattened `backends/*`, installed the plist, and hit the **networked**
branch of the profile message with a resolvable path (the #58 fix,
independently verified on this hardware in the prior `#56`/`#60` leg):

```
It then pulls the configured generate + embed models into the CAS
store. Watch progress with:  /Users/…/inferd-v0.8.0-aarch64-apple-darwin/inferdctl watch
```

All three models (`gemma-4-e4b` 4888 MiB, `gemma-4-e4b-mmproj` 944 MiB,
`embeddinggemma-300m` 313 MiB) fetched cleanly from the pinned-revision
URLs with **no SHA mismatch** — confirming the Linux leg's #64 fix
(immutable repo revisions instead of `resolve/main/`) holds on macOS's
independent HTTPS stack too. `inferdctl doctor`: every check `ok`.

```
status=ready, accelerator=metal, device=MTL0 vram=11.8 GiB
backend: embeddinggemma-300m — gpu_layers=25 embed=true
backend: gemma-4-e4b          — gpu_layers=43 vision=true audio=true
                                 audio_sample_rate=16000 tools=true
                                 thinking=true
```

`gpu_layers=43` (E4B) / `25` (embed) — **a third exact match** to the
Windows CUDA and Linux CUDA figures, confirming the #51 fix is not just
cross-*vendor* (CUDA/Metal) but produces the identical offload count
regardless of accelerator, which is the strongest form of "host
independent" evidence yet.

`inferdctl status`: exit 0, both capabilities frames plus the `ready`
snapshot, all fields present — confirms #57/#61 live on Metal, third
platform.

Auto-select was not exercised: this config carries the explicit two-backend
shape (not `model_autoselect: "auto"`), same as it does on every platform
this doc covers, so E4B is here because it is the pinned default, not
because auto-select chose it. That branch remains proven only by the ADR
0023 unit tests and the v0.6.1 log message recorded in the earlier doc —
worth naming plainly rather than implying the 16-vs-20-GiB threshold was
re-exercised.

### Baseline — the install=work claim

`packaging/validate/wire.py` resolved `$TMPDIR/inferd/inferd.sock` and
`…/infer.embed.sock` correctly — confirms the macOS-specific path (not
the Linux `~/.inferd/run` fallback) is live in the harness, not just
documented.

- **generate** — `'INSTALLWORK080'` verbatim, `stop_reason=end_turn`,
  `backend=llamacpp`.
- **embed** — 256 dims (MRL truncation), **L2 = 1.000000**.

### G-A..G-H — identical results to both prior legs

| Gate | Result |
|---|---|
| G-A `required` → real call | `get_weather({"city":"Dublin"})`, `stop_reason=tool_use` |
| G-B adversarial `required` | `text='hi\n```\nhi\n```\n'`, 0 calls, `stop_reason=max_tokens`, **`tool_choice_unsatisfied=true`** |
| G-C unpaired `tool_result` | `invalid_request`, naming `"call-that-never-happened"` |
| G-D `tool_choice` without `tools` | `invalid_request`: "requires a non-empty tools array" |
| G-E `tool_choice` + `response_format` | `invalid_request`, mutual-exclusion message |
| G-F unknown `tool_choice` string | `invalid_request`: "must be one of: auto, required, none" |
| G-G `none` | 0 calls; control with no `tool_choice` made 1 call; "2 + 2" → `'4'` so text stayed reachable |
| G-H paired `tool_result` | `'The weather in Dublin is sunny and 21°C.'` |

G-B reproduced the identical degenerate repetition (`'hi\n```\nhi\n```\n'`)
byte-for-byte on a third platform and a third accelerator vendor — this is
now clearly a property of the constraint against this prompt, not of any
one host.

### Environmental finding: G-B needed a longer client-side read timeout on this box

Not a daemon defect, but worth recording plainly since it looked like one
at first. `packaging/validate/gates.py` (unmodified, per the issue's
instruction not to rewrite the committed harness) uses a 180s
per-request client read timeout. G-B — the adversarial `required` prompt
that forces the model to run to its full 64-token budget — exceeded that
consistently on this run, while every other gate (including G-A, which
also calls the model) completed in seconds.

Root cause, established by manually sending G-B's exact request with a
300s socket timeout and watching the daemon log concurrently: this
16 GiB box was under genuine memory pressure during the run (`PhysMem: 15G
used, 9GB wired + 3GB compressor`, load average 2.3–4.0, several stray
`inferd-http` processes left resident from earlier unrelated validation
sessions on this same machine). Metal shader pipeline JIT-compiles new
kernel-shape variants on first use (`ggml_metal_library_compile_pipeline:
compiling pipeline …`), which is a real one-time cost this box's earlier
gates had already partly paid, but the combination with active memory
compression made one particular decode run take several minutes rather
than seconds. The daemon was never stuck — `inferdctl doctor` stayed
`ready` throughout — and the eventual result was byte-for-byte correct
(`stop_reason=MaxTokens`, `tool_choice_unsatisfied=true`, matching G-B's
expected shape exactly).

After killing the two stray leftover `inferd-http` processes and re-running
both `gates.py` and `bridge.py` through a small **wrapper** that only
widens the client read timeout (600s) without editing either committed
script, every gate passed on the first attempt, including G-B. The
wrapper's code is not part of this repo and was discarded after the run;
it changed nothing about the request/response wire, only how long the
Python client was willing to wait for it.

### Bridge (`inferd-http`) from the archive

| Gate | Result |
|---|---|
| `/health` | `{"status":"ok"}` |
| `/v1/models` | `{"data":[{"id":"inferd",…}],"object":"list"}` |
| non-stream chat | `BRIDGE080` verbatim, `finish_reason: "stop"` |
| stream chat | 34 data lines ending `[DONE]` |
| `/v1/embeddings` | 256 dims, L2 1.000000 |
| **G7 `stream:false`** | `tool_calls` array present, `content: null`, `finish_reason: "tool_calls"` |
| **G7 `stream:true`** | terminal chunk carries `tool_calls`, `finish_reason: "tool_calls"` |
| G7 round trip | returned `message` echoed back with a `role:"tool"` result → `'The weather in Dublin is sunny and 21°C.'` |
| named-function `tool_choice` | HTTP 400 with the documented message |

The `3b75846` non-streaming fix is confirmed on a third platform, from
the archive binary, on both paths.

**Additional OpenAI SDK pass** (this box has the SDK installed,
`openai==2.45.0`), run against the same bridge instance after the
`bridge.py` pass: non-stream chat (`'SDKCHECK080'`, `finish_reason=stop`),
streaming with the SDK's default streaming behaviour (`'1\n2\n3'`),
embeddings float (768 dims — the SDK's un-truncated default, vs. the
harness's explicit 256-dim MRL request) and base64 (returns a `str`, as
the SDK expects), and a `tool_choice="required"` non-stream call
(`message.content is None`, `message.tool_calls` populated with the
`Function` object correctly typed). No SDK-specific defects found — the
two bugs the README warns an SDK pass can catch (stream default, base64)
were already fixed by the time of this leg.

## Result — macOS arm64 Metal

Green, no new defects. Every v0.8.0 surface — `tool_choice` enforcement
including the adversarial case, `tool_choice_unsatisfied`, the breaking
`tool_result` rejection and its paired-path counter-proof, and both
`inferd-http` code paths for tool calls — behaved identically to Windows
CUDA and Linux CUDA. The `gpu_layers` figures (43/25) now match across
three accelerator implementations, and the #57/#58/#61/#64 fixes are each
independently confirmed live on a third platform. The one thing this leg
adds beyond confirmation: real evidence that Metal's first-use shader JIT
compilation, combined with host memory pressure, can push a single
adversarial-prompt generation well past a 180s client timeout without any
daemon-side problem — worth knowing before reading a timeout on this
platform as a hang.

Remaining before a tag: the two arm64 legs (Linux aarch64, Windows arm64).
